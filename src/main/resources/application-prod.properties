# ============================================================
# CONFIGURACIÓN DE PRODUCCIÓN PARA RAILWAY
# ============================================================

# Puerto dinámico
server.port=${PORT:8080}

# Conexión a MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configuración de Hikari para manejar conexiones
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.connection-test-query=SELECT 1

# Vaadin
vaadin.productionMode=true
vaadin.pnpm.enable=false

# Logging optimizado
logging.level.root=ERROR
logging.level.com.ElihuAnalytics.ConsultorioAcupuntura=WARN
logging.level.org.springframework=ERROR
logging.level.com.vaadin=ERROR
logging.level.org.hibernate=ERROR
logging.level.org.springframework.orm.jpa=ERROR
logging.pattern.console=%d{HH:mm:ss} %-5level %logger{36} - %msg%n

# Compresión HTTP
server.compression.enabled=true
server.compression.mime-types=text/html,text/css,application/javascript,application/json

# Configuración alternativa si DATABASE_URL no está disponible
spring.datasource.url=jdbc:mysql://$rafael-N53SM:$3306/$consultorio?useSSL=false&serverTimezone=UTC
spring.datasource.username=$root
spring.datasource.password=$Melindrosa1971!