# ============================================================
# CONFIGURACIÓN DE PRODUCCIÓN PARA RAILWAY
# ============================================================

# Puerto dinámico
server.port=${PORT:8080}

# Conexión a MySQL con DATABASE_URL
spring.datasource.url=jdbc:${DATABASE_URL}&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=15000&socketTimeout=30000&autoReconnect=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Hikari: pool de conexiones
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.connection-test-query=SELECT 1

# Vaadin
vaadin.productionMode=true
vaadin.pnpm.enable=false

# Excluir Neo4j
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.neo4j.Neo4jDataAutoConfiguration,org.springframework.boot.autoconfigure.data.neo4j.Neo4jRepositoriesAutoConfiguration

# Logging optimizado
logging.level.root=ERROR
logging.level.com.ElihuAnalytics.ConsultorioAcupuntura=WARN
logging.level.org.springframework=ERROR
logging.level.com.vaadin=ERROR
logging.level.org.hibernate=ERROR
logging.level.org.springframework.orm.jpa=ERROR
logging.pattern.console=%d{HH:mm:ss} %-5level %logger{36} - %msg%n

# Compresión HTTP
server.compression.enabled=true
server.compression.mime-types=text/html,text/css,application/javascript,application/json